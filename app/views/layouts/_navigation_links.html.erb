<%# add navigation links to this file %>
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Categories <b class="caret"></b></a>
            <ul class="dropdown-menu">
                <% Bot::CATEGORIES.each do |category|%>
                  <li><%= link_to category, bot_directory_path(category: category) %></li>
              <%end%>
            </ul>
        </li>
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Plateformes <b class="caret"></b></a>
            <ul class="dropdown-menu">
                <% Bot::PLATFORMS.each do |platform|%>
                  <li><%= link_to platform, bot_directory_path(platform: platform) %></li>
              <%end%>
            </ul>
        </li>
<li><%= link_to 'About', page_path('about') %></li>
         <li>
            <%= link_to 'Contact', page_path('contact') %>
        </li>

