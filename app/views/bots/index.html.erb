
<%= render 'search_box' %>
<%= render 'layouts/messages' %>
<!-- Page Content -->
<div class="container">
    <!-- /.row -->
    <div class="col-md-12">
        <!-- Sidebar Column -->
        <sidebar class="col-md-3 sidebar">
          <div class="panel panel-primary">
            <div class="panel-heading">Plateformes</div>
            <div class="list-group">
              <%= link_to "Toutes les plateformes", bot_directory_path(category: @category, language: @language), class: 'list-group-item' %>
              <% Bot::PLATFORMS.each do |platform|%>
                  <%= link_to platform.gsub('_', ' '), bot_directory_path(platform: platform, category: @category, language: @language), class: 'list-group-item' %>
              <%end%>
            </div>
          </div>
          <div class="panel panel-primary">
            <div class="panel-heading">Catégories</div>
            <div class="list-group">
              <%= link_to "Toutes les catégories", bot_directory_path(platform: @platform, language: @language), class: 'list-group-item' %>
              <% Bot::CATEGORIES.each do |category|%>
                  <%= link_to category, bot_directory_path(category: category , platform: @platform, language: @language), class: 'list-group-item' %>
              <%end%>
            </div>
          </div>
          <div class="panel panel-primary">
            <div class="panel-heading">Langues</div>
            <div class="list-group">
             <%= link_to "Toutes les langues", bot_directory_path(platform: @platform, category: @category), class: 'list-group-item' %>
              <% Bot::LANGUAGES.each do |language|%>
                  <%= link_to language, bot_directory_path(category: @category , platform: @platform, language: language), class: 'list-group-item' %>
              <%end%>
            </div>
          </div>

        </sidebar>
        <div class="col-md-9">
          <div class='index-header'>
            <h1><%=@title%></h1>
            <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Soluta, et temporibus, facere perferendis veniam beatae non debitis, numquam blanditiis necessitatibus vel mollitia dolorum laudantium, voluptate dolores iure maxime ducimus fugit.</p>
          </div>

            <%= render @bots %>

          <div class='row clearfix col-sm-12 text-center'>
          <br>
            <%= will_paginate @bots, class:'apple_pagination' %>
            <%=link_to 'Soumettre un Nouveau Bot', new_bot_path, class: 'btn btn-primary'%>
          </div>
        </div>
    </div>

</div>